pipeline {
    agent any
    tools {
        maven 'Maven'
    }
    stages {
        stage('build') {
            steps {
                echo "stage building..."
                
            }
        }
        stage('test') {
            steps {
                echo "stage testing..."
            }
        }

        stage('deploy') {
            steps {
                echo "stafe deploying..."
            }
        }
    }
}
