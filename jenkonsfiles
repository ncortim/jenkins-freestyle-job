pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "stage building..."
                
            }
        }
        stage('test') {
            steps {
                echo "stage testing..."
            }
        }

        stage('deploy') {
            steps {
                echo "stafe deploying..."
            }
        }
    }
}
